import React, {Component} from 'react';

//by convention the class name is same as the file name
class App extends Component {
    // Component class already takes care of attaching the render method to 'this' object
    render(){
        return (
            <div>
                <h1>Create React App</h1>
            </div>
        )
    }
}

export default App;

